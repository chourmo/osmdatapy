name: test
channels:
  - conda-forge
  - defaults

dependencies:
    # Base
  - python>=3.9
  - pip
  - numpy
  - pandas
  - geopandas
  - pygeos
  - cython

    # Datasource
  - aiohttp


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Development
  - cython

    # Pip-only installs
  #- pip:
  #  - codecov

