name: test
channels:

  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - aiohttp
  - geopandas
  - pygeos

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

