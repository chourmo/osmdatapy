name: test
channels:
  - conda-forge
  - defaults

dependencies:
    # Base
  - python>=3.9
  - numpy
  - pandas
  - geopandas
  - pygeos

    # Datasource
  - aiohttp

    # Testing
  - pip
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
