name: osmdatapy_dev
channels:
  - conda-forge

dependencies:
    # Base
  - python>=3.9
  - numpy
  - pygeos
  - pandas
  - geopandas

    # Datasource
  - aiohttp

    # Development
  - black
  - cython
  - ipykernel

    # Testing
  - pytest-cov
  - codecov
  - pytest
prefix: /opt/homebrew/Caskroom/miniforge/base/envs/osmdatapy_dev
